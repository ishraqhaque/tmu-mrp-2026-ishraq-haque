# Copy this file to `.env` and fill in your key. `.env` is gitignored — never commit it.
# Get your key from https://site.financialmodelingprep.com/developer/docs (Dashboard → API Keys)
FMP_API_KEY=your_fmp_api_key_here
